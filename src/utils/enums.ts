export enum EnvEnum {
  DEVELOPER = "development",
  PRODUCTION = "production",
  TEST = "test",
}


export enum providersEnum {
  GOOGLE = "google",
  LOCAL = "local",
  SYSTEM = "system",
}

export enum UserRole {
  ADMIN = "admin",
  USER = "user",
}